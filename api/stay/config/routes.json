{
  "routes": [
    {
      "method": "GET",
      "path": "/stays",
      "handler": "stay.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/stays/count",
      "handler": "stay.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/stays/:id",
      "handler": "stay.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/stays",
      "handler": "stay.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/stays/:id",
      "handler": "stay.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/stays/:id",
      "handler": "stay.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
